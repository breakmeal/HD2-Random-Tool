# PLANS.md

本文件规定本项目今后如何编写和维护复杂任务的 Execution Plan（ExecPlan）。ExecPlan 是可执行、持续更新的工作记录，不是一次性方案说明。

## 何时必须建立 ExecPlan

出现以下任一情况时，应在实施前建立独立 ExecPlan：

- 复杂功能或用户可见行为存在多个验收条件。
- 修改跨越多个业务文件或同时涉及 HTML、CSS、JavaScript、数据和生成脚本。
- 大型重构、数据模型迁移、持久化格式变化或脚本加载关系变化。
- 需要运行 `collect-wiki-data.mjs` 并审查大量生成结果。
- 工作包含多个阶段、显著风险、外部数据依赖或尚未解决的技术选择。
- 任务预计无法在一个清晰、短小的修改步骤中完成。

简单的文案修正、单一局部样式调整、明确的小型数据更正或纯文档改动通常不需要独立 ExecPlan，但仍应执行 `AGENTS.md` 要求的验证。

默认将独立计划保存为 `plans/<简短任务名>.md`。当前尚无 `plans/` 目录；只有出现第一个需要独立计划的复杂任务时才创建，不应为占位而新增空目录。计划文件命名使用小写英文和连字符。团队是否有其他计划归档位置为“待确认”。

## ExecPlan 的基本原则

- **自包含**：只阅读计划和仓库即可理解任务，不依赖聊天记录中的隐含信息。
- **基于现状**：所有文件、命令、统计和限制都必须先从当前项目确认。
- **面向结果**：写清用户可观察到的结果，而不只是内部实现动作。
- **持续更新**：计划必须随着实施同步变化，不能只在开始时填写一次。
- **记录原因**：重要选择不仅记录“选了什么”，还要记录原因和影响。
- **可验证**：每项需求和主要步骤都应能对应到明确的验证方式。
- **尊重生成边界**：区分手写源文件、生成文件和下载资源。
- **诚实处理未知项**：无法确认的内容写为“待确认”，不得自行假设。

## 必需章节

每份 ExecPlan 至少包含以下章节，标题可附中文解释，但英文名称应保留。

### Purpose / Goal

说明为什么要做、最终用户会得到什么，以及任务成功时可观察到的状态。应简洁描述范围，不在这里展开实现细节。

### Requirements

列出必须满足的功能、数据、兼容性和非功能要求。要求应可验收，并注明明确不在范围内的事项。

适合采用如下格式：

```markdown
- [ ] 用户可以……
- [ ] 未发布装备仍然不会……
- [ ] 手机宽度下……
- Out of scope: ……
```

### Current State

记录实施前的真实状态，包括：

- 相关文件、函数、数据流和脚本加载顺序。
- 当前行为和已知限制。
- 现有测试、校验命令与文档。
- 手写文件、生成文件和外部来源之间的关系。
- 与任务有关的数量或基线结果，例如装备数、图片数和校验摘要。

不得凭记忆填写；应引用具体路径和必要的符号名。

### Implementation Plan

按依赖顺序列出具体实施阶段。每个阶段说明：

- 要修改的文件或模块。
- 要实现的行为。
- 为什么采用该顺序。
- 完成该阶段后如何快速验证。

步骤应足够具体，让未参与前期讨论的人可以继续执行，但不要复制大段预期代码。

### Progress

这是执行中的实时状态记录。使用复选框或状态标记，并在重要更新后注明日期/时间。

```markdown
- [x] 2026-08-08：确认当前数据模型和生成边界。
- [ ] 修改用途分类逻辑。
- [ ] 完成桌面与手机回归。
```

至少在以下时点更新：

- 完成一个实施阶段后。
- 范围、顺序或验收标准改变后。
- 出现阻塞、失败尝试或新的依赖后。
- 作出重要技术决定后。
- 得到验证结果后。

不得在任务结束时一次性补写成“看起来一直正确”的历史记录。

### Decisions

以决策日志记录重要选择。每条至少包含日期、决定、原因和影响。

```markdown
- 2026-08-08 — 决定：……
  - 原因：……
  - 影响：……
```

应记录的内容包括数据模型选择、兼容方案、生成策略、弃用方案和范围取舍。小型语法选择无需记录。

### Discoveries

记录执行中发现且会影响实现或后续维护的事实，例如：

- 文档与代码不一致。
- 数据源字段含义与预期不同。
- 某文件实际为生成文件。
- 当前结构存在覆盖层、隐式全局依赖或兼容性限制。
- 校验暴露了新的缺图、孤立详情或 ID 变化。

每项发现应附证据，如文件路径、函数名、命令摘要或可靠来源。不要粘贴冗长原始日志。

### Risks / Open Questions

列出可能影响交付的风险、尚未验证的假设和需要用户决定的问题。每项注明：

- 影响。
- 当前缓解措施。
- 是否阻塞实施。
- 负责人或所需输入；无法确定时标记“待确认”。

风险解决后不要直接删除，应保留并标注解决方式。

### Validation

在实施前写出计划执行的验证，在执行后补充真实结果。至少区分：

- 静态/语法检查。
- 数据完整性校验。
- 浏览器交互验证。
- 响应式验证。
- 回归验证。

本项目当前可用的基础命令是：

```powershell
node --check <修改过的 JavaScript 或 MJS 文件>
node scripts/validate-data.mjs
py -m http.server 4173
```

没有现成的 Lint 或自动化浏览器测试，计划不得虚构这些结果。数据采集任务还应记录 `collect-wiki-data.mjs` 的最终摘要和抽查对象。

### Outcome

任务完成时填写，包含：

- 实际交付内容。
- 与最初 Requirements 的差异。
- 最终验证结果。
- 遗留问题或后续工作。
- 是否产生新的生成数据、资源或兼容性影响。

任务未完成时也要说明当前结果和阻塞原因，不能把未实现内容写成已完成。

## 推荐的 ExecPlan 模板

```markdown
# <任务名称> ExecPlan

状态：Draft | Active | Blocked | Complete
创建日期：YYYY-MM-DD
最后更新：YYYY-MM-DD HH:mm

## Purpose / Goal

## Requirements

## Current State

## Implementation Plan

## Progress

## Decisions

## Discoveries

## Risks / Open Questions

## Validation

## Outcome
```

## 执行与维护规则

1. 先完成 Current State 的只读调查，再确定 Implementation Plan。
2. 开始改代码前，Purpose、Requirements、Current State、Implementation Plan、初始 Risks 和 Validation 必须可用。
3. 实施期间将计划视为工作台账；发生重要变化时先或同时更新计划。
4. 如果需求改变，更新 Requirements，并在 Decisions 中记录范围变化原因。
5. 如果步骤失败，记录失败事实和后续策略，不要抹去失败路径。
6. 如果发现任务比预期简单，可以缩减未执行步骤，但必须在 Decisions 说明。
7. 如果简单任务扩展为跨文件或高风险工作，应立即创建或升级为独立 ExecPlan。
8. 不在计划中保存密码、令牌、Cookie 或其他敏感信息。
9. 不把大段代码、完整生成文件或长日志复制进计划；用路径和摘要引用。
10. 完成任务前逐项核对 Requirements 与 Validation，并填写 Outcome。
11. 完成后的计划保留作为维护记录，不把它改写成一般产品文档；稳定知识应另行同步到 `AGENTS.md`、`README.md` 或 `SOURCES.md`。

## 与其他文档的关系

- `AGENTS.md`：长期有效的项目规则、命令、边界和完成标准。
- `PLANS.md`：ExecPlan 的格式和维护方法。
- `README.md`：面向使用者和一般开发者的项目说明。
- `SOURCES.md`：数据、图片和字段来源。
- `plans/*.md`：单个复杂任务的执行历史和结果。

ExecPlan 中发现的长期规则应反馈到 `AGENTS.md`，但不得删除或覆盖 README/SOURCES 中仍有用的原始内容。

## 待确认事项

- ExecPlan 是否需要指定固定审批人或审阅人。
- 已完成计划的归档周期和命名保留策略。
- 是否未来接入 issue、PR 或项目管理系统并与 ExecPlan 互链。
